 # Porter configuration — https://porter.run
# Detected automatically when deploying from a GitHub repo.
name: teamdev-terminal
type: dockerfile
dockerfile: Dockerfile
port: 8080
env:
  PORT: "8080"
  TERMINAL_PASSWORD: "R@b1u2004@"
  KEEPALIVE_URL: ""
healthCheck:
  path: /health
  port: 8080
  intervalSeconds: 30
  timeoutSeconds: 10
  retries: 3